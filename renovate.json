{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommitsDisabled"
  ],
  "timezone": "America/Los_Angeles",
  "packageRules": [
    {
      "description": "qBittorrent (LinuxServer): track only the clean X.Y.Z app-version tag. The regex versioning drops the 5.2.3_v2.0.14-lsNNN / -libtorrentv1 variants; allowedVersions drops the ancient 20.04.x Ubuntu-scheme tags (raise the major cap if qBittorrent ever reaches v10).",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["lscr.io/linuxserver/qbittorrent"],
      "versioning": "regex:^(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)$",
      "allowedVersions": "/^[0-9]\\.[0-9]+\\.[0-9]+$/"
    },
    {
      "description": "AutoBangumi: SemVer, so beta tags are ignored",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["ghcr.io/estrellaxd/auto_bangumi"],
      "versioning": "semver"
    },
    {
      "description": "Group the caddy base image bumps into one PR",
      "matchFileNames": ["_caddy-tailscale/Dockerfile"],
      "groupName": "caddy base image"
    }
  ]
}
