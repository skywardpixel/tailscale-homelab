# Per-project secrets / host-specific config. Templates are committed.
.env
!*/.env.example

# Personal subscription list. config.example.yaml is committed instead.
animegakill/config.yaml

# Local compose tweaks (image overrides, debug ports) — host-specific.
compose.override.yaml
compose.override.yml

# OS / editor cruft
.DS_Store
*.swp
*~
