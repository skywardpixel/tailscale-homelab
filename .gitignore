# Per-project secrets / host-specific config. Templates are committed.
.env
!*/.env.example

# Local compose tweaks (image overrides, debug ports) — host-specific.
compose.override.yaml
compose.override.yml

# OS / editor cruft
.DS_Store
*.swp
*~

# Python bytecode from local linting/compiling
__pycache__/

# Bastion deployment identities and local operator notes.
/bastion/access-policy.local.json
/bastion/README.local.md
